import(methods)
importFrom(utils, head, tail)
export(kmeans.big.matrix, colmean, add.cols, as.big.matrix, big.matrix, biglm.big.matrix, bigglm.big.matrix, hash.mat, is.big.matrix, deepcopy, read.big.matrix, write.big.matrix, rm.cols, colsd, colvar, mwhich, colmin, colmax, colrange, colsum, colprod, is.shared)
exportClasses(big.matrix)
exportMethods("[", "[<-", dimnames, "dimnames<-", mean, min, max, range, prod, sum, nrow, ncol, summary, print, head, tail, typeof)

useDynLib(bigmemory) # Was .so

